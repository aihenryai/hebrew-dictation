{
  "name": "hebrew-dictation-api",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker API for Hebrew Dictation v2.0 — proxies transcription requests to Groq/Deepgram with per-device quota tracking",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "typescript": "^5.7.0",
    "wrangler": "^3.100.0"
  }
}
